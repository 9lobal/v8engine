Если Параметры.Свойство("ТекущееЗначение") И ЗначениеЗаполнено(Параметры["ТекущееЗначение"]) Тогда
	
	Значение = Параметры["ТекущееЗначение"];
	Для Каждого Итр Из ДеревоТаблиц.ПолучитьЭлементы() Цикл
		
		Если Итр.ИмяТаблицы = Значение Тогда
			Элементы.ДеревоТаблиц.ТекущаяСтрока = Итр.ПолучитьИдентификатор();
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
КонецЕсли;